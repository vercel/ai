import Root from './button.svelte';
import {
  buttonVariants,
  type ButtonProps,
  type ButtonSize,
  type ButtonVariant,
} from './variants.js';

export {
  Root,
  Root as Button,
  buttonVariants,
  type ButtonProps,
  type ButtonProps as Props,
  type ButtonSize,
  type ButtonVariant,
};
