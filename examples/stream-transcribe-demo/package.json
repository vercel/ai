{
  "name": "@example/stream-transcribe-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Local browser demo for streaming speech-to-text via experimental_streamTranscribe (OpenAI gpt-realtime-whisper + xAI WebSocket STT).",
  "scripts": {
    "prestart": "pnpm --filter ai... --filter @ai-sdk/openai... --filter @ai-sdk/xai... build",
    "start": "node --env-file-if-exists=.env server.mjs"
  },
  "dependencies": {
    "@ai-sdk/openai": "workspace:*",
    "@ai-sdk/xai": "workspace:*",
    "ai": "workspace:*",
    "ws": "^8.18.0"
  }
}
