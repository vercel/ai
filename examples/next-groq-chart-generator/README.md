# Next.js + Groq + Recharts Example

This example demonstrates how to build a Generative UI that renders interactive charts using [Groq](https://groq.com) and [Recharts](https://recharts.org).

## Setup

1. Copy the example env file:
   ```bash
   cp .env.example .env.local

   Add your Groq API Key to .env.local: 
   GROQ_API_KEY=gsk_..

   Install dependencies and run:
   npm install
   npm run dev

   ### **Step 5: Create `.env.example`**
1.  Right-click your new folder again.
2.  **New File** → Name it **`.env.example`**.
3.  Paste this inside:
    ```env
    GROQ_API_KEY=
    ```