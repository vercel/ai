{
  "version": 2,
  "issueId": 21124,
  "expectationRevision": 1,
  "command": "pnpm -C examples/ai-functions exec tsx src/reproduction/use-object-overlapping-stop.ts",
  "artifactPaths": [
    "examples/ai-functions/src/reproduction/use-object-overlapping-stop.ts"
  ],
  "expectedFailure": {
    "exitCode": 1,
    "signal": "ISSUE_21124_REPRODUCED: isLoading became false while the newer request was active; stop() did not abort the newer request; the newer response updated the object after stop(); clear() did not abort the newer request; the newer response repopulated the object after clear()"
  }
}
