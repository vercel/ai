---
'ai': patch
---

Add part-level UI message metadata validation, extend tool approval and denial metadata in streamed UI chunks, and align agent UI stream helper typings with `UIMessage` part metadata.
