---
'@ai-sdk/sandbox-local-workspace': patch
---

feat (sandbox-local-workspace): HarnessV1SandboxProvider that runs harnesses on the local machine, reusing the user's installed CLI configuration, credentials and native tools, scoped to a caller-supplied project directory.
