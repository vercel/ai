---
'ai': patch
'@ai-sdk/react': patch
'@ai-sdk/vue': patch
'@ai-sdk/svelte': patch
'@ai-sdk/angular': patch
---

Keep a replacement completion request cancellable and loading when an earlier request finishes or aborts. Prevent older requests from overwriting the replacement request's completion and error state.
