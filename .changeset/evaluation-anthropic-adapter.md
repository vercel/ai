---
'@ai-sdk/anthropic': patch
---

Add `anthropic.evaluationModel()` for experimental Choice and Score evaluations through Messages structured output, with exact labels, validated score bounds, and Boolean rejection before I/O.
