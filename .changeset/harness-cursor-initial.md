---
"@ai-sdk/harness-cursor": patch
---

feat(harness): add Cursor harness adapter (`@ai-sdk/harness-cursor`)

Bridge-backed local adapter for `@cursor/sdk` that runs Cursor agents inside the AI SDK sandbox and streams harness-compatible events over WebSocket.
