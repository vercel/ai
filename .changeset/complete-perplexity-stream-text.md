---
'@ai-sdk/perplexity': patch
---

Recover missing text from Agent API terminal events and output items, including incomplete responses, without repeating text already received as deltas. Track each message content part separately.
