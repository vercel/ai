---
'ai': patch
---

feat (ai): re-export realtime server-connection and usage types

Adds `RealtimeServerConnection`, `RealtimeSessionIntent`, and `RealtimeUsage` aliases for the new experimental `RealtimeModelV4` server-side spec types.
