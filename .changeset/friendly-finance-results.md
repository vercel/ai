---
'@ai-sdk/perplexity': patch
---

Accept native Agent API tool traces such as finance results without validating them as web search results. Preserve these traces in raw responses and stream chunks.
