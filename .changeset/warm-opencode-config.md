---
'@ai-sdk/harness-opencode': patch
---

Apply changed native OpenCode configuration when reusing a warm bridge. Preserve the existing session, reuse unchanged runtimes, and allow retrying a failed configuration refresh.
