---
'@ai-sdk/xai': patch
---

Add video extension and reference-to-video (R2V) support with explicit `mode` option (`edit-video`, `extend-video`, `reference-to-video`). Legacy provider options without `mode` remain backward compatible.