---
'ai': patch
'@ai-sdk/react': patch
---

Add the native realtime WebSocket session runtime with lifecycle management, bounded event queues, audio capture and playback, and backend command coordination. Keep the existing React realtime store compatible with optional session state through internal callback bookkeeping.
