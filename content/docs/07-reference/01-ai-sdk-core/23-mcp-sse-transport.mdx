---
title: SSEMCPTransport
description: Create a transport for Model Context Protocol (MCP) clients to communicate with MCP servers using HTTP and Server-Sent Events
---

# `SSEMCPTransport`

Creates a transport for Model Context Protocol (MCP) clients to communicate with MCP servers using HTTP and Server-Sent Events.

This feature is experimental and may change or be removed in the future.

## Import

<Snippet text={`import { SSEMCPTransport } from "ai"`} prompt={false} />

## API Signature

### Parameters

<PropertiesTable
  content={[
    {
      name: 'config',
      type: 'SSEConfig',
      description: 'Configuration for the MCP client.',
      properties: [
        {
          type: 'SSEConfig',
          parameters: [
            {
              name: 'url',
              type: 'string',
              description: 'The URL of the MCP server.',
            },
          ],
        },
      ],
    },
  ]}
/>
