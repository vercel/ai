---
title: useChat
description: Reference documentation for the useChat hook in the AI SDK UI
---

# `useChat`

Allows you to easily create a conversational user interface for your chatbot application. It enables the streaming of chat messages from your AI provider, manages the state for chat input, and updates the UI automatically as new messages are received.

## Import

### React

<Snippet text={`import { useChat } from "ai/react"`} prompt={false} />

### Svelte

<Snippet text={`import { useChat } from "ai/svelte"`} prompt={false} />

### Vue

<Snippet text={`import { useChat } from "ai/vue"`} prompt={false} />

### Solid

<Snippet text={`import { useChat } from "ai/solid"`} prompt={false} />

<ReferenceTable packageName="react" functionName="useChat" />
