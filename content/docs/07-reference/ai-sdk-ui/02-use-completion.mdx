---
title: useCompletion
description: Reference documentation for the useCompletion hook in the AI SDK UI
---

# `useCompletion`

Allows you to create text completion based capibilities for your application. It enables the streaming of text completions from your AI provider, manages the state for chat input, and updates the UI automatically as new messages are received.

## Import

### React

<Snippet text={`import { useCompletion } from "ai/react"`} prompt={false} />

### Svelte

<Snippet text={`import { useCompletion } from "ai/svelte"`} prompt={false} />

### Vue

<Snippet text={`import { useCompletion } from "ai/vue"`} prompt={false} />

### Solid

<Snippet text={`import { useCompletion } from "ai/solid"`} prompt={false} />

<ReferenceTable packageName="react" functionName="useCompletion" />
