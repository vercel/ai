---
title: Crusoe
description: Learn how to use Crusoe's models with the AI SDK.
---

# Crusoe Provider

The [Crusoe](https://crusoecloud.com) provider offers access to language models through the [Crusoe Intelligence Foundry](https://docs.crusoecloud.com/managed-inference/overview), powered by Crusoe's clean energy cloud infrastructure.

API keys can be obtained from the [Crusoe Cloud Console](https://console.crusoecloud.com/foundry/api-keys).

## Setup

The Crusoe provider is available via the `@ai-sdk/crusoe` module. You can install it with:

<Tabs items={['pnpm', 'npm', 'yarn', 'bun']}>
  <Tab>
    <Snippet text="pnpm add @ai-sdk/crusoe" dark />
  </Tab>
  <Tab>
    <Snippet text="npm install @ai-sdk/crusoe" dark />
  </Tab>
  <Tab>
    <Snippet text="yarn add @ai-sdk/crusoe" dark />
  </Tab>
  <Tab>
    <Snippet text="bun add @ai-sdk/crusoe" dark />
  </Tab>
</Tabs>

## Provider Instance

You can import the default provider instance `crusoe` from `@ai-sdk/crusoe`:

```ts
import { crusoe } from '@ai-sdk/crusoe';
```

For custom configuration, you can import `createCrusoe` and create a provider instance with your settings:

```ts
import { createCrusoe } from '@ai-sdk/crusoe';

const crusoe = createCrusoe({
  apiKey: process.env.CRUSOE_API_KEY ?? '',
});
```

You can use the following optional settings to customize the Crusoe provider instance:

- **baseURL** _string_

  Use a different URL prefix for API calls.
  The default prefix is `https://api.inference.crusoecloud.com/v1`.

- **apiKey** _string_

  API key that is being sent using the `Authorization` header. It defaults to
  the `CRUSOE_API_KEY` environment variable.

- **headers** _Record&lt;string,string&gt;_

  Custom headers to include in the requests.

- **fetch** _(input: RequestInfo, init?: RequestInit) => Promise&lt;Response&gt;_

  Custom [fetch](https://developer.mozilla.org/en-US/docs/Web/API/fetch) implementation.

## Language Models

You can create language models using a provider instance:

```ts
import { crusoe } from '@ai-sdk/crusoe';
import { generateText } from 'ai';

const { text } = await generateText({
  model: crusoe('meta-llama/Llama-3.3-70B-Instruct'),
  prompt: 'Write a vegetarian lasagna recipe for 4 people.',
});
```

Crusoe language models can also be used in the `streamText` function
(see [AI SDK Core](/docs/ai-sdk-core)).

You can also use the `.languageModel()` and `.chat()` methods:

```ts
const model = crusoe.languageModel('meta-llama/Llama-3.3-70B-Instruct');
const model = crusoe.chat('meta-llama/Llama-3.3-70B-Instruct');
```

## Model Capabilities

| Model                                         | Image Input         | Object Generation   | Tool Usage          | Tool Streaming      |
| --------------------------------------------- | ------------------- | ------------------- | ------------------- | ------------------- |
| `deepseek-ai/DeepSeek-V3-0324`                | <Cross size={18} /> | <Check size={18} /> | <Check size={18} /> | <Check size={18} /> |
| `deepseek-ai/DeepSeek-V4-Flash`               | <Cross size={18} /> | <Check size={18} /> | <Check size={18} /> | <Check size={18} /> |
| `deepseek-ai/DeepSeek-V4-Pro`                 | <Cross size={18} /> | <Check size={18} /> | <Check size={18} /> | <Check size={18} /> |
| `google/gemma-4-31b-it`                       | <Check size={18} /> | <Check size={18} /> | <Check size={18} /> | <Check size={18} /> |
| `meta-llama/Llama-3.3-70B-Instruct`           | <Cross size={18} /> | <Check size={18} /> | <Check size={18} /> | <Check size={18} /> |
| `nvidia/Nemotron-3-Nano-30B-A3B`              | <Cross size={18} /> | <Check size={18} /> | <Check size={18} /> | <Check size={18} /> |
| `nvidia/Nemotron-3-Nano-Omni-Reasoning-30B-A3B` | <Check size={18} /> | <Check size={18} /> | <Check size={18} /> | <Check size={18} /> |
| `nvidia/Nemotron-3-Super-120B-A12B`           | <Cross size={18} /> | <Check size={18} /> | <Check size={18} /> | <Check size={18} /> |
| `openai/gpt-oss-120b`                         | <Cross size={18} /> | <Check size={18} /> | <Check size={18} /> | <Check size={18} /> |
| `qwen/Qwen3-235B-A22B`                        | <Cross size={18} /> | <Check size={18} /> | <Check size={18} /> | <Check size={18} /> |
| `zai/GLM-5.1`                                 | <Cross size={18} /> | <Check size={18} /> | <Check size={18} /> | <Check size={18} /> |

<Note>
  The table above reflects capabilities based on publicly available documentation.
  You can also pass any available provider model ID as a string if needed.
  Please see the [Crusoe Intelligence Foundry docs](https://docs.crusoecloud.com/managed-inference/overview)
  for the most up-to-date list of models.
</Note>
