# AI SDK - Baidu Provider

The Baidu provider for the AI SDK is currently being scaffolded in this package.

## Setup

The provider will be available in the `@ai-sdk/baidu` module.

```bash
npm i @ai-sdk/baidu
```

## Status

This package currently contains the initial scaffolding only. Provider APIs and model support will be added in follow-up tasks.
