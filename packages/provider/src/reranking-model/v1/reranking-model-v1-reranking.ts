/**
A RerankIndex is a index of reranking documents.
It is e.g. used to represent a index of original documents that are reranked.
 */
export type RerankingModelV1DocumentIndex = Array<number>;

/**
 * A RerankingDocuments is a list of reranked documents.
 *
 */

export type RerankingModelV1RerankingDocuments = Array<string>;
