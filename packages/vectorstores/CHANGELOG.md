# @ai-sdk/vectorstores

## 0.0.1

### Initial Release

- Added `vectorstores` tool for querying vectorstores.org's VectorStoreIndex
- Added `vercelEmbedding` adapter for using AI SDK embedding models with vectorstores.org
