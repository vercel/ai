// @ts-nocheck
import { createAnthropicClientOptions } from 'ai';

const opts = {
  baseURL: 'https://api.anthropic.com',
};

const otherOpts = {
  baseURL,
};

const config = {
  something: baseURL,
};