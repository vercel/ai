# AI SDK - Konsistent Conventions

Reusable [konsistent](https://github.com/vercel-labs/konsistent) conventions for the AI SDK monorepo.

This package exports the structural conventions enforced across packages in this repository (e.g. required package files, provider symbol exports, model file naming) so that they can be consumed via `@konsistent/convention` from the root `konsistent.json` configuration.
