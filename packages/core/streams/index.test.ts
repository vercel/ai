import { AIStream } from '.'
describe('ai', () => {
  beforeEach(() => {
    // scanReturnValues = [[0, []]];
  })

  describe('not a real test', () => {
    it('exports a default client', () => {
      expect(true).toEqual(true)
    })
  })
})
